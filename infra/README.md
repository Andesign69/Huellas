# infra

Deployment configuration for running Huellas on the production VPS: `Dockerfile`, `docker-compose.yml` (prod + staging services), and the nginx reverse-proxy config.

Not yet populated — this lands in WBS Chapter 7 (Containerization & Deploy Pipeline). See `docs/adr/` for the reasoning behind self-hosting instead of Vercel/Supabase.
